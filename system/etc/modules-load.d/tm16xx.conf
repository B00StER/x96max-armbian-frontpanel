# Force-load the SPI glue at boot. The panel device's modalias is spi:fd628 but
# the module advertises spi:tm1628, so udev coldplug won't auto-load it; once
# loaded, the driver binds the device by its OF "compatible" string.
tm16xx_spi
